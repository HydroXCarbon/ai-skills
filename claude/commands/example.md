---
description: Template slash command — replace this with what the command does.
allowed-tools: Read, Bash(git status:*), Bash(git diff:*)
argument-hint: "<optional args>"
---

# /example

You invoked this command with: `$ARGUMENTS`

Replace this body with the actual prompt Claude should run when the
command is used. Keep it focused on a single intent.
